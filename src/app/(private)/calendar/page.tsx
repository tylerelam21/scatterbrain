export default function CalendarPage() {
  return (
    <main className="mx-auto w-full max-w-5xl flex-1 px-6 py-16">
      <h1 className="text-2xl font-semibold tracking-tight font-display">Calendar</h1>
      <p className="mt-2 text-muted">
        Placeholder — Google Calendar connection lands in Phase 3.
      </p>
    </main>
  );
}
