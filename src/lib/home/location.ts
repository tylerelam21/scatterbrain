// Static for now — a personal, decorative header detail rather than the
// location-aware suggestions PRD §6 defers to V2 (Discover/Wander). Could
// become a stored AppSetting later if it's ever worth making configurable.
export const HOME_CITY = "ATL";
export const HOME_LAT = 33.749;
export const HOME_LON = -84.388;
